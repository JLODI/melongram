<% if flash.any?%>
  <div class="alert alert-success text-center" role="alert">

  <%# flash notice %>
    <% flash.each do |type, message| %>
      <p class="notice mb-0 <%= type %>"><%= message %></p>
    <% end %>
  </div>
<% end %>